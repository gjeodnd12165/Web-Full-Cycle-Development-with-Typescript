const arr = [1, 2, 3, 4, 5];

const forEached = arr.forEach((v, i, a) => {
  return v * 2;  
});

const mapped = arr.map((v, i, a) => {
  return v * 2;
});

console.log(`${forEached} | ${mapped}`);

let map = new Map();
map.forEach((v, k ,m) => {

});

